# This must always remain the same for the Windows installer infrastructure
# to know which program to upgrade.
UPGRADE_GUID = faad5686-7606-4e63-9433-87eee3e3a170

# If we set this then it doesn't matter what the directory's called.
HUMAN_PROJECT_NAME = Jujitsu

include make/simple.make
